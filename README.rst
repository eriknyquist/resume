Setup (Linux)
=============

Install required latex packages:

::

    sudo apt install texlive texlive-latex-extra

Build
=====

Run the following command to get a PDF version of the resume:

::

    pdflatex -jobname=erik_nyquist_cv erik_nyquist_cv.tex
